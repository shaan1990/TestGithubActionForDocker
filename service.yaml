apiVersion: v1
kind: Service
metadata:
  name: myapp-service
spec:
  type: NodePort
  selector:
    app: myapp
  ports:
    - port: 8081        # external service port
      targetPort: 8081  # matches container port
      nodePort: 32036   # optional fixed NodePort
